# Text Directory

This directory stores manuscript binaries or local writing assets, such as `.docx` drafts.

These files are intentionally ignored by Git by default because binary manuscript files tend to create noisy version history.

Project-level scientific narratives should preferentially be documented in Markdown when possible.

# External Directory

This directory is reserved for third-party assets and external repositories used by the project.

It is intentionally not tracked by the parent repository by default.

Important note:

- `external/SpatialValidation` is an external dependency and should be treated as its own repository, not as source code owned by this project.

Any external dependency required for reproducibility should be documented here and in the main project README.

# Results Directory

This directory stores generated experiment outputs such as:

- benchmark summaries
- model comparison tables
- spatial predictions
- intermediate CSV files
- diagnostic outputs

These files are intentionally ignored by Git by default to keep the repository lightweight and focused on source code.
